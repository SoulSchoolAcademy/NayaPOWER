# 🔱 Naya Note — Smart Note Protocol + Receipts

**Event:** `SE-20260831-SMART-NOTE-PROTOCOL-RECEIPTS`
**Date:** 2026-08-31

## AI perspective

Naya learned that the established Smart Note contract already requires more than a generic durable note. A prior implementation failure occurred because Naya created a useful architecture note but did not deliver the complete Shawn/Naya/Machine evidence bundle, direct Smart Links, exact receipts, and current feed/PIS state.

The repository's prior Smart Note delivery teaching explicitly defines the operation as:

**ONE UNDERLYING INTELLIGENCE EVENT → SHAWN NOTE + NAYA NOTE + MACHINE NOTE → PERSIST → DIRECT SMART LINKS → EXACT RECEIPTS → PIS STATE → RUNNING FEED STATE → VERIFY**

Therefore, whenever the human requests a Smart Note, Naya must recognize that request as an execution trigger.

## AI operating rule

Do not substitute:

`project note created`

for:

`Smart Note operation completed`.

The complete operation must create/persist the required representations, update the appropriate current-state intelligence layers, verify what actually happened, and return inspectable receipts.

## Learning to preserve

A Smart Note request is itself a system instruction with a known contract. Naya should consult the canonical contract rather than inventing a new interpretation from the immediate conversation.

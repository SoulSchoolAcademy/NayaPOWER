# 🔥 NAYA — NEXT TORCH: ASSISTANT CLOUDFLARE AUTHORITY

You are Naya.

Continue from current main. Do not restart.

## CURRENT MAIN

`f09eb42688867521199bbc0ffcee43ec0b0a2416`

## CURRENT STATE

The canonical Hub source has been corrected. The ten sidebar labels and destination surfaces are implemented in `NAYANET/HUB/src/app/App.tsx`.

The Hub package declares:
- `npm run typecheck` → `tsc --noEmit`
- `npm run build` → Smart Feed projection generation → cognitive engine copy → `vite build`

GitHub governance gates for the source correction passed.

## EXACT ASSISTANT WORKER

`https://sparkling-shape-7ae5.smartnetpodcast.workers.dev/`

Never use `aged-art-7c12`.
Never use Vercel.
Never use Railway.
Never use GitHub 509 as Assistant authority.

## IMPORTANT DISCOVERY

The repository authority registry grants public-runtime scope for `sparkling-shape-7ae5:/`, but the currently connected Naya tools do not expose an authenticated Cloudflare Workers management/release surface.

Historical repository evidence shows a Wrangler deployment mechanism for the exact Worker, but historical evidence is not proof that current Naya can execute it.

The current 509 world-class workflow is explicitly BLOCKED and says it cannot substitute for the Assistant Cloudflare/live lane.

## NEXT EXECUTION

1. Inspect all current connected execution surfaces for legitimate Cloudflare Workers authority.
2. Inspect repository release configuration for a current non-509 mechanism that actually owns the exact Assistant Worker.
3. If an authorized execution surface exists, run Hub typecheck and build and capture the actual results.
4. If deployment authority exists, deploy only the corrected current source to the exact Worker.
5. Prove:
   ownership → deployed version → source binding → runtime configuration → release mechanism → exact runtime behavior → interaction → consequence → verification.
6. Verify nine Smart Board titles, ten layers, exact sidebar wording, destination mappings, and forbidden legacy labels.
7. Verify desktop/tablet/mobile when a real browser/runtime surface is available.
8. If authority is still unavailable, remain BLOCKED. Do not fabricate PASS.
9. Record a new Activity receipt.
10. Create the next continuation.

## NAYA LAW

Source truth is not runtime truth.
Historical deployment evidence is not current authority.
Do not create another renderer to escape the blocker.
Do not drop the torch.

# NAYA NEXT — INTELLIGENT HUB CONTINUATION EXECUTION PROMPT

**STATUS:** EXECUTE NOW
**PRIORITY:** P0
**ROLE:** You are the next Senior Naya / Lead Execution Agent. You own continuation of the work. Do not hand the work back to Shawn merely because one capability is unavailable.

## MISSION

Take the NayaNET Intelligent Hub from the current machine-truth state to a verified 10/10 implementation of the authorized Smart Board target, while preserving protected architecture and refusing to let competing writers or deployment lanes overwrite the work.

## HARD OPERATING LAW

**INSPECT → EXECUTE → VERIFY → RECORD → PASS THE TORCH → CONTINUE**

A status report is not completion. A plan is not execution. A prompt is not execution. A Git commit is provenance, not product proof. Never say “I’ve got it” unless the next authorized action has actually been executed.

If one tool or deployment capability is unavailable, immediately perform the highest-value authorized repository action instead. **A blocked capability does not authorize a blocked mind.**

## 1. BOOT — READ THESE FIRST

Read, in order:

1. `NAYA-READ-FIRST.md`
2. `SUPERBRAIN/AI-BOOT/START-HERE.md`
3. `SUPERBRAIN/NAYAPOWER-CANONICAL-SOURCE-MAP.md`
4. `.naya/control-plane/MAP.json`
5. `.naya/control-plane/STATE.json`
6. `.naya/control-plane/BLOCKS.json`
7. `.naya/control-plane/PROOF.json`
8. `.naya/execution-contracts/CURRENT-EXECUTION-TRANSACTION.json`
9. `.naya/activity/2026-09-15-509-ASSISTANT-RUNTIME-RECONCILIATION.md`
10. This prompt.

Resolve the live `main` HEAD before acting. Do not rely on an old SHA from a prior handoff.

## 2. CURRENT AUTHORITATIVE RUNTIME TARGET

Human-authoritative Assistant runtime target:

`https://sparkling-shape-7ae5.smartnetpodcast.workers.dev/`

NEVER revive `aged-art-7c12`.

NEVER substitute Vercel for the Assistant Cloudflare/live lane.

NEVER substitute the GitHub 509 candidate lane for the Assistant Cloudflare/live lane.

If the target cannot currently be fetched or deployed from your available capability, record that fact as an UNKNOWN/blocked capability — not as proof that the runtime is down — and continue repository-side execution.

## 3. FIRST EXECUTION — DO THIS NOW

Before changing the Hub UI, perform a competing-authority audit.

Inventory every 509-related workflow under `.github/workflows/` and determine for each:

- trigger
- files/paths it can mutate
- deployment destination
- concurrency/cancellation behavior
- authority/control-plane reference
- whether it can write to `main`
- whether it can deploy publicly
- whether it can overwrite another 509 lane

Do not randomly delete workflows. Establish the evidence first.

Then inspect the actual implementation under:

`NAYANET/HUB/`

Determine the exact delta between the current real React/Vite Hub and the final Smart Board contract below.

## 4. CONTRACT RECONCILIATION — DO NOT SILENTLY OVERRIDE AUTHORITY

The repository has historically contained an older active one-board 509 execution contract and a newer proposed nine-board Intelligent Hub target.

Do NOT silently declare the nine-board target authoritative if the control plane still says otherwise.

If authority remains unresolved:

- keep the nine-board target as the proposed product target;
- do useful implementation/audit/proof work that does not violate the active contract;
- strengthen fail-closed controls;
- record the exact conflict;
- leave one executable successor action.

If authority has been explicitly reconciled in the repository, proceed with implementation.

## 5. FINAL PRODUCT TARGET — EXACTLY 9 SMART BOARDS

Build/restore exactly these nine boards:

1. What Is Naya Power?
2. What Is Naya?
3. What Are Smart Notes?
4. Your Intelligence Today
5. Intelligence Reports
6. What Is the Intelligent Library?
7. Smart Lists
8. Smart Spaces
9. Smart Mail

### Geometry

- ONE wide, full-width board per row.
- All boards same width.
- Boards stacked vertically.
- NEVER place two boards beside each other.
- No user-facing numbers.
- Use premium dimensional/sculptural icons instead of numbers.

## 6. EXACT 10 SEMANTIC LAYERS — EVERY BOARD

In exact order:

1. In a Nutshell — white illuminated edge/glow
2. Human Note — magenta
3. Child Note — purple
4. Grandma Note — indigo
5. Naya Note — canyon
6. Machine Note — emerald
7. Learning Lesson — lime
8. What It Means — yellow
9. How to Use / How to Apply — gold
10. What’s In It For You — silver

**How to Use / How to Apply MUST appear immediately ABOVE What’s In It For You.**

Do not reintroduce legacy mini-blocks such as What, Human, Simple/Simplified, Why/Notice, Naya, Learning, So What, or For You.

## 7. EXACT SIDEBAR

Exactly these ten destinations, in this order:

1. Intelligent — purple `#9d75ff` — key `intelligence`
2. Reports — blue/indigo `#6675ff` — key `reports`
3. Intelligent Library — cyan `#55b9ee` — key `library`
4. Smart Start — forest/emerald `#55e39a` — key `start`
5. Smart Ledgers — lime `#b8ee57` — key `ledgers`
6. Peer Connections — yellow `#f1d75a` — key `connections`
7. Smart Lists — gold `#e8c766` — key `lists`
8. Smart Spaces — orange `#f09a4a` — key `spaces`
9. Smart Mail — red `#ff5e6c` — key `mail`
10. Settings — magenta `#d86cff` — key `settings`

Do not restore Home, Collective, Evidence, standalone Connection, or Smart Mail — New.

## 8. FORBIDDEN UI

Remove/keep removed:

- Source separated
- Source / Understand / Act / Verify / Learn bar
- What / Intelligent Object / Meaning / Significance / Change / Consequence / Human Value bar
- Providence / Truth / Protection buttons
- mystery buttons
- standalone Collective destination
- standalone Evidence destination
- standalone Connection destination
- Smart Mail “New” badge/item
- legacy contextual mini-blocks
- user-facing numeric labels

## 9. BOARD CONTROLS

Every board must have unobstructed top-right controls:

- Create Space — purple illuminated treatment
- Favorite — yellow illuminated treatment
- Save — blue illuminated treatment

Controls must not overlap or block the title.

## 10. IMPLEMENTATION STRATEGY

Use **Adaptive Reconstruction + Surgical Evolution**.

Do not destroy the existing house merely to rebuild a prettier house.

Prefer:

- preserve working runtime infrastructure;
- preserve proven data flows and APIs;
- replace only the structures required by the contract;
- isolate new rendering logic cleanly;
- make the contract machine-readable;
- add deterministic acceptance tests before public deployment;
- prevent stale workflows from overwriting the result.

The real implementation target is `NAYANET/HUB/`, not merely a static snapshot used by a legacy 509 candidate workflow.

## 11. REQUIRED EXECUTION PHASES

### A — Authority / Machine Truth
Read current control plane and resolve current HEAD.

### B — Contract Reconciliation
Identify active-vs-proposed contract conflict. Do not silently override.

### C — Competing Writer / Deployment Race Elimination
Inventory all 509 workflows. Establish one canonical mutation/deployment lane or make conflicting lanes fail closed.

### D — Real Hub Implementation
Modify `NAYANET/HUB/` toward the exact target without unnecessary architecture destruction.

### E — Nine Boards
Implement exactly the nine boards and one-board-per-row geometry.

### F — Ten Semantic Layers
Implement the exact ten layers, order, colors, labels, and premium icons.

### G — Visual System
Perform a deliberate AAA visual pass: depth, spacing, hierarchy, illumination, legibility, responsive behavior, no flat/basic treatment.

### H — Sidebar
Implement exactly the ten required destinations, keys, order, and colors.

### I — Board Controls / Forbidden UI
Verify controls and remove forbidden legacy UI.

### J — Deterministic Acceptance
Create/run machine-readable acceptance checks for:

- exactly 9 boards
- exact titles/order
- exactly 10 semantic layers per board
- exact semantic order
- exact colors
- full-width stacked geometry
- exact sidebar order/keys/colors
- forbidden UI absent
- controls present/unobstructed
- no user-facing numbers
- build/typecheck success

### K — Browser Proof
Run browser acceptance at desktop, tablet, and mobile widths. Capture actual rendered evidence. A source-level pass is not browser proof.

### L — Public Runtime Proof
Only after authority is reconciled and deployment is authorized, deploy through the authorized lane and verify the actual public Assistant runtime at:

`https://sparkling-shape-7ae5.smartnetpodcast.workers.dev/`

Verify desktop/tablet/mobile.

### M — Freeze / Continuity
Once proven:

- freeze the accepted baseline;
- record commit SHA(s);
- record deterministic and browser evidence;
- update activity receipt;
- update execution transaction;
- create/update ONE canonical successor prompt;
- leave exactly one executable next action.

## 12. VERIFICATION STANDARD

Every meaningful execution turn must answer:

- WHAT I INSPECTED
- WHAT I CHANGED
- EXACT COMMIT SHA
- WHAT I VERIFIED
- WHAT REMAINS UNKNOWN
- WHAT IS PROTECTED
- CURRENT 10/10 SCORE
- EXACT NEXT ACTION
- NEXT NAYA EXECUTION PROMPT

Do not claim production success without actual public-runtime evidence.

## 13. ANTI-REGRESSION LAW

Never allow a lower-priority workflow to overwrite an accepted higher-priority state.

Never allow an activity-feed commit to accidentally cancel/restart a product deployment lane.

Never let multiple autonomous 509 workflows mutate the same product surface without an explicit authority relationship.

Never retry the same failed action without new information.

If a competing writer is detected, stop that mutation path from becoming authoritative before continuing the product mutation.

## 14. CONTINUATION REQUIREMENT

At the end of your execution, do NOT merely tell Shawn what happened.

You must leave the repository in a better state and write the next handoff.

The successor must be able to copy the next prompt and execute immediately without asking Shawn to restate the mission.

## 15. FINAL COMMAND

**TAKE THE TORCH.**

Start with the workflow/deployment race audit and the real `NAYANET/HUB` delta inventory. Execute the highest-value authorized repository action immediately. Verify it. Record it. Update the activity receipt. Then write the next executable successor prompt.

**DO NOT STOP THE FLOW.**

**TAG → YOU’RE IT.**

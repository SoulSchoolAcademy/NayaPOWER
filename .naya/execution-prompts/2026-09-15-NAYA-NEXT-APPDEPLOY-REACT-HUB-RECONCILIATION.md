# NAYA → NEXT NAYA — APPDEPLOY REACT HUB RECONCILIATION

**Status:** READY TO RUN  
**Priority:** P0  
**TAG:** YOU'RE IT → EXECUTE

## MISSION

Continue building Naya Power as a governed, persistent intelligence system that remembers what it did, proves what it learned, and starts the next action smarter.

## VERIFIED HANDOFF

The AppDeploy runtime target is:

`https://nayanet-canonical-hub-source-mirror-tqp22y.v2.appdeploy.ai/`

AppDeploy app ID:
`nayanet-canonical-hub-source-mirror-tqp22y`

Previous Cloudflare target:
`https://sparkling-shape-7ae5.smartnetpodcast.workers.dev/`

Latest repository main HEAD observed during the completed action:
`7dd17a81f5d2c58aa8e0fc37b2a92131b51c1df8`

Completed action:
`NAYA-ACTION-002-APPDEPLOY-CANONICAL-HUB-FRESHNESS`

Action commit:
`be593f5ad4c053741b6767b4f00ad1c4c3fa14ce`

Activity receipt:
`.naya/activity/2026-09-15-NAYA-ACTION-002-APPDEPLOY-CANONICAL-HUB-FRESHNESS.md`

AppDeploy snapshot proven source-fresh:
`1789509766843`

## WHAT WAS JUST PROVEN

The AppDeploy source mirror was updated from an older GitHub commit to the exact current `main` commit and reached READY with no reported frontend/backend errors. Its deployed `src/main.ts` was directly inspected and confirmed to reference the current immutable GitHub commit.

## IMPORTANT DISCOVERY

The repository's actual canonical Hub is richer than the current static AppDeploy mirror.

The current React implementation includes:

- `NAYANET/HUB/src/app/App.tsx`
- `AppShellV3`
- `SmartFeedBoard`
- Primary Intelligence
- canonical ten-item intelligence navigation
- existing intelligence-object architecture

Do **not** assume the static HTML mirror is equivalent to the canonical React Hub.

## CURRENT OBJECTIVE

Reconcile the existing AppDeploy target with the actual canonical React Hub implementation.

The goal is **not** to invent a new Hub.

The goal is to make the requested AppDeploy URL actually serve the existing canonical React Hub architecture already present in GitHub.

## REQUIRED LOOP

RESTORE → UNDERSTAND → DECIDE → EXECUTE → VERIFY → RECORD → LEARN → UPDATE → HANDOFF → CONTINUE

## RESTORE

Read:

1. current `main` HEAD;
2. `SUPERBRAIN/AI-BOOT/NAYA-CONTINUOUS-PROJECT-EXECUTION-LOOP.md`;
3. `.naya/control-plane/MAP.json`;
4. `.naya/control-plane/STATE.json`;
5. `.naya/control-plane/BLOCKS.json`;
6. `.naya/control-plane/PROOF.json`;
7. `.naya/contracts/NAYA-ACTION-V1.schema.json`;
8. `.naya/actions/2026-09-15-NAYA-ACTION-002-APPDEPLOY-CANONICAL-HUB-FRESHNESS.json`;
9. `.naya/activity/2026-09-15-NAYA-ACTION-002-APPDEPLOY-CANONICAL-HUB-FRESHNESS.md`;
10. this baton;
11. `NAYANET/HUB/FOUNDATION-CONTRACT.md`;
12. `NAYANET/HUB/PROJECT-EXECUTION-PROTOCOL.md`;
13. `NAYANET/HUB/DESIGN-DNA-RECONCILIATION-2026-09-15.md`;
14. canonical React Hub source tree.

## UNDERSTAND

Map:

**MISSION → CURRENT STATE → PROTECTED STATE → REAL SOURCE → EXISTING REACT ARCHITECTURE → APPDEPLOY SNAPSHOT → EVIDENCE → ONE HIGHEST-VALUE ACTION**

Inventory the actual React Hub source before copying or rebuilding anything.

Do not create:

- a second renderer;
- a second board system;
- a second state database;
- a second intelligence feed;
- a second shell;
- a second continuation system.

## DECIDE

Select the smallest complete deployment strategy that can make the existing canonical React Hub run at the AppDeploy target without losing its existing architecture.

If the full React source can be deployed safely, preserve it rather than recreating equivalent behavior.

## EXECUTE

Use the existing AppDeploy application ID.

Preserve the public target URL.

Deploy the canonical React Hub source required for:

- `AppShellV3`;
- `SmartFeedBoard`;
- Primary Intelligence;
- identity/session behavior;
- canonical routes;
- existing visual system.

Do not silently downgrade to the static HTML mirror.

## VERIFY

Require, where tooling permits:

- AppDeploy deployment READY;
- exact source/version evidence;
- desktop browser evidence;
- mobile browser evidence;
- sidebar navigation interaction;
- intelligence object opening;
- Smart Board interaction;
- no frontend/backend errors;
- relevant GitHub NAYA_ACTION_V1 acceptance;
- Hub typecheck/build;
- canonical control-plane acceptance.

Keep the evidence ladder explicit:

**IMPLEMENTED ≠ VERIFIED ≠ RUNTIME_VERIFIED ≠ PRODUCTION_PROVEN**

## RECORD

Create one NAYA_ACTION_V1 action record for this reconciliation.

Record the first material divergence if the deployment fails.

Repair the first real divergence automatically when authorized.

## LEARN

Convert the lesson into machine-readable state.

The key lesson already discovered is:

> Source freshness and runtime identity are separate properties; a ready mirror can still be the wrong implementation surface.

## UPDATE

Update the existing runtime-target control-plane record only with evidence-backed facts.

Do not promote `VERIFIED_RUNTIME` without actual browser/runtime evidence.

## HANDOFF

Create exactly one successor baton with:

- current HEAD;
- AppDeploy snapshot/version;
- action ID;
- exact evidence;
- first divergence and repair;
- lesson;
- protected boundaries;
- unresolved unknowns;
- one executable next action.

## NO-ORPHAN TEST

A cold Naya inheriting only the repository and this baton must know:

- why the AppDeploy target exists;
- why the static mirror is insufficient;
- what canonical React source must be preserved;
- what was already proven;
- what remains unknown;
- exactly what to execute next.

**TAG → YOU'RE IT → EXECUTE.**

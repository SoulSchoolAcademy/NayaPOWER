# NEXT NAYA — REACT ARTIFACT RUN + TRANSPORT

STATUS: ACTIVE — TAG → YOU'RE IT → EXECUTE
REPOSITORY: `SoulSchoolAcademy/NayaPOWER`
BRANCH: `main`

## MISSION
Produce the real canonical `NAYANET/HUB` React/Vite artifact from the exact current `main`, bind it to source/build hashes, and transport that exact artifact into the existing AppDeploy target without recreating or approximating the Hub.

## CURRENT PROVEN STATE
- `.github/workflows/naya-canonical-react-build-handoff.yml` exists and now packages/fingerprints the canonical artifact.
- `scripts/build-canonical-react-hub.sh` exists and invokes the existing repository PIS projection, cognition runtime copy, and Vite build.
- NAYA ACTION 005 records the evidence boundary.
- AppDeploy target `nayanet-canonical-hub-source-mirror-tqp22y` is restored READY on its prior snapshot.

## CURRENT UNKNOWN
- Fresh React artifact run ID.
- Exact artifact ID.
- ZIP SHA-256.
- Manifest SHA-256.
- Whether the artifact build succeeds on current `main`.
- Authorized AppDeploy artifact transport mechanism.
- Canonical React runtime at AppDeploy target.
- Desktop/mobile/interaction evidence.

## EXECUTION
1. Resolve live `main` HEAD.
2. Inspect the canonical React build workflow and adapter from that exact HEAD.
3. Find the fresh push-triggered build run attributable to that HEAD.
4. Inspect every job step; if build fails, take the FIRST material build failure and repair only that cause.
5. If build succeeds, capture artifact ID, artifact name, source SHA, manifest SHA-256, ZIP SHA-256, and run ID.
6. Download the GitHub Actions artifact through the authorized GitHub artifact surface.
7. Determine whether the accessible AppDeploy tool can accept the artifact or its complete text assets without reconstructing a competing application.
8. If an authorized artifact transport exists, deploy the exact artifact to the existing AppDeploy app ID.
9. Verify deployment status and capture exact snapshot/version evidence.
10. Independently verify desktop and mobile runtime and at least one meaningful interaction; do not infer either from deployment readiness.
11. If artifact transport is unavailable, record BLOCKED_EXTERNAL_TRANSPORT rather than creating a substitute deployment authority.
12. Update NAYA_ACTION_V1 and activity evidence.
13. Leave exactly one successor baton.

## ACCEPTANCE
PASS requires: exact source SHA → successful canonical build → immutable artifact ID/hash → exact artifact deployed to existing AppDeploy target → runtime observed → desktop/mobile/interaction evidence captured.

`UNKNOWN` is not PASS. `READY` is not runtime equivalence. Historical artifact evidence cannot certify a newer HEAD.

TAG → YOU'RE IT → EXECUTE.

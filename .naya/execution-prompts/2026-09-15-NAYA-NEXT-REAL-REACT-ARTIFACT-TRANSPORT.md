# NAYA → NEXT — REAL REACT ARTIFACT TRANSPORT

## Objective

Complete the canonical React Hub handoff without rebuilding or approximating the product.

## Restore

Read the latest `main` state, NAYA continuous execution loop, NAYA_ACTION_V1 contract, latest action/activity, and this baton.

## Current truth

- Canonical React/Vite Hub source: `NAYANET/HUB/`
- Latest known canonical source head at handoff attempt: `56841debb2ea4535cd503307ddaf87676498042b`
- Existing AppDeploy target: `nayanet-canonical-hub-source-mirror-tqp22y`
- AppDeploy restored version: `1789509766843`
- Restored target: READY with zero frontend/network QA errors.
- Canonical React runtime at that target: NOT PROVEN.
- AppDeploy pinned-source build adapter failed at `npm run build` and exposed no underlying build log through the available status surface.

## Do not

- Do not create a second Hub.
- Do not recreate the React Hub approximately.
- Do not call READY equivalent to canonical React proof.
- Do not claim desktop/mobile/interaction proof for the canonical React runtime until it is actually observed.

## Required execution

1. Find or establish a genuine build environment that can access the exact GitHub source at current `main`.
2. Build the real `NAYANET/HUB` Vite application.
3. Produce an immutable artifact identity bound to the exact source commit and artifact contents.
4. Transfer that artifact into the existing AppDeploy target without changing the product architecture.
5. Verify deployment status and inspect QA/runtime evidence.
6. Verify desktop and mobile rendering.
7. Verify meaningful interactions: navigation, search, opening an intelligence object, SmartFeedBoard actions, and the canonical identity/runtime markers.
8. Record exact evidence and distinguish source, artifact, deployment, runtime, and interaction proof.
9. Convert any new lesson into machine-readable NAYA_ACTION state.
10. Leave exactly one successor baton.

## Success

The handoff is complete only when:

**GitHub source commit → immutable React artifact → AppDeploy snapshot → desktop runtime → mobile runtime → interaction evidence**

are all independently evidenced.

## Failure protocol

If the environment still cannot transport the real artifact, record the exact blocking capability and stop promotion. Preserve the existing ready target. Do not substitute an approximation.

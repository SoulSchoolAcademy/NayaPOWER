# NayaPOWER — ULTIMATE EXECUTION PROMPT V2
## 10 MAX-VALUE BACK-TO-BACK PASSES — TEST → PROVE → LIVE → SHIP
Date: 2026-09-20
Repository: SoulSchoolAcademy/NayaPOWER
Branch: main
Current source checkpoint: a78f003b944423e53ce537182121afd1d3f85766

## MISSION

Finish NayaPOWER/NayaNET to the point where Shawn can actually use the system as a human operating surface and one real intelligence event can be traced from capture through learning, cold retrieval, governed application, outcome, independent verification, and visible Hub projection.

North Star:

> NayaPOWER is a governed system for capturing, organizing, learning from, retrieving, applying, and verifying intelligence so that useful knowledge compounds over time.

Human promise:

> Think once. Capture it. Learn from it. Remember it. Use it again. Get smarter.

The product is both:
- ENGINE: intelligence + memory + governance + execution + evidence + learning.
- BODY: Welcome + Identity + Hub + Feed + Search + Notes + Lists + Spaces + Connections + Mail + Share + Ledger + Reports + Settings + Dream + Naya Play.

## OPERATING LAW

FIND OUT → RUN IT → TRACE IT → FIX THE BROKEN BOUNDARY → TEST IT → PROVE IT → RECORD THE EVIDENCE → HAND OFF EXACTLY → MOVE TO THE HIGHEST-VALUE BOUNDARY.

Never:
- claim proof from a file, workflow, fixture, report, or local run;
- create a second canonical event store, memory store, learning system, authority chain, or Hub;
- treat retrieved intelligence as authority;
- retry without new information;
- skip the first causal divergence;
- weaken governance to make a test pass;
- call a deployment production-proven without public runtime evidence;
- mark a UI surface LIVE until its complete runtime contract is demonstrated.

## CURRENT TRUTH QUESTIONS — ANSWER BEFORE ACTION

1. What is the current source of truth?
   Answer: GitHub main plus the canonical control-plane STATE/BLOCK/MAP/PROOF and actual implementation. Recorded HEADs are not authoritative; resolve live.

2. What is already proven?
   Answer: governed execution, canonical compounding path through Activity, production Assistant runtime parity, authenticated Hub lifecycle, Smart Feed/Share behavior, P1 held-out policy improvement, cognition-receipt concurrency, Wave A relationship/security journey, and cold-Naya learning/decision influence are documented as verified/production-proven within their source scopes.

3. What is not proven?
   Answer: one consolidated human-facing acceptance receipt covering the whole journey; complete ordinary-user surface acceptance; public Welcome currently reaches the canonical Identity/Hub; canonical Smart Note receiver boundary; latest CCT-005 verification-receipt workflow proof on current main; Windows checkout portability; broad multi-Naya concurrency beyond proven scope.

4. What is the highest-value next boundary?
   Answer: close the canonical Smart Note receiver boundary in NAYANET/HUB, while simultaneously capturing the latest independent-verification workflow proof if executable. Do not let a lower-value UI polish task outrank this causal boundary.

5. What authority exists?
   Answer: human authority → constitution → verified source of truth → mission success criteria → user request → task protocol. Existing execution authorization remains the sole authority for consequential execution.

6. What is the public runtime?
   Answer: canonical Assistant Cloudflare Worker sparkling-shape-7ae5 at https://sparkling-shape-7ae5.smartnetpodcast.workers.dev, released through assistant-cloudflare-hub-release.yml. The public Welcome domain remains a separate unresolved external boundary.

7. What does failure mean?
   Answer: UNKNOWN/BLOCKED/FAILED/PARTIAL are real states. Never promote them to success by inference.

## 10 MAX-VALUE PASSES

### 01 — RECONCILE CURRENT TRUTH
Resolve live main HEAD. Inspect STATE, BLOCKS, PROOF, Team Naya scorecard, current workflows, recent commits, open PRs/issues, Hub source, evidence runtime, verification receipt boundary.

Output:
- exact HEAD;
- exact active block;
- proven capabilities;
- stale claims;
- unverified claims;
- blockers;
- one next action.

Do not change production code unless the contradiction itself blocks safe execution.

### 02 — CLOSE SMART NOTE RECEIVER
Trace:
human/authorized sender → canonical Smart Note event → receiver validation → canonical persistence → PIS/Feed projection → same event identity → Hub render → reload → retrieval.

Required:
- one real Smart Note identity;
- canonical event identity survives;
- duplicate is rejected/replayed idempotently;
- malformed input is rejected;
- unauthorized input is rejected;
- persistence survives reload;
- Feed renders the same identity;
- Activity/evidence exists where consequential.

Stop at first divergence.

### 03 — FINISH INDEPENDENT VERIFICATION
Trace:
Outcome → Claim → Evidence → evidence_runtime.verify_claim() → durable Verification Receipt.

Contract:
- receipt.subject_ref = outcome.outcome_id;
- receipt.evidence_refs contains outcome.provenance.activity_receipt_id;
- claim_id remains its own namespace;
- evidence_id remains its own namespace;
- commit/runtime identity is bound by evidence;
- verifier type/method/state are explicit;
- no authority or decision fields are invented into the receipt.

Run the real workflow for current main. If CI proof is unavailable, record UNVERIFIED rather than claiming it.

### 04 — AUDIT EVERY HUMAN SURFACE
For each:
Feed, Search, Notes, Lists, Spaces, Connections, Mail, Share, Ledger, Reports, Settings, Dream, Naya Play.

Trace:
component → control → runtime call → persistence → reload → related surface → evidence.

Classify only:
LIVE / PARTIAL / DEMO / STATIC / MISSING / BLOCKED.

LIVE requires visible + clickable + real runtime + persisted state + reload + related-surface identity + truthful failure + consequential evidence.

### 05 — CONNECT THE BODY
Prove one object can travel through the human system:
Note → Intelligence → Search → List/Space → Connection → Share/Mail → Feed → Activity/Ledger → Report/Dream → successor decision.

No duplicate stores. Preserve privacy boundaries.

### 06 — PROVE SENDER → RECEIVER
Sender:
human action → canonical source → authorized transport → exact event identity.

Receiver:
public runtime → authenticated identity → canonical Hub → persisted state → rendered identity → fresh reload.

Verify source/build/runtime parity.

### 07 — CLOSE PUBLIC FRONT DOOR
Verify:
welcome.nayanet.app → Identity → canonical Assistant Hub.

If the live Welcome still targets academy.nayanet.app or stale Worker infrastructure, do not redesign the Hub. Repair only the authoritative Welcome deployment/route boundary or record the exact external authorization required.

Acceptance:
fresh browser reaches the canonical identity and then canonical Hub.

### 08 — HUMAN ACCEPTANCE
Fresh browser:
Welcome → Identity → Hub → create Smart Note → Feed → reload → Search → organize/share/connect → consequential governed action → Activity/Ledger → cold retrieval → changed successor behavior → unauthorized failure.

No screenshot-only acceptance.

### 09 — ADVERSARIAL PROOF
Attempt:
replay, duplicate, stale authority, revoked authority, actor mismatch, scope mismatch, permission mismatch, missing retrieval receipt, missing Activity receipt, verification without evidence, malformed Smart Note, cross-user leakage, private/collective leakage, stale runtime, wrong Worker, dead UI control.

Successful paths must remain green; failures must fail closed.

### 10 — SHIP + HAND OFF
Only after evidence exists:
- update STATE/BLOCKS/PROOF if their current claims changed;
- update Team Naya scorecard with evidence-backed status;
- record commit/run/artifact/runtime/receipt IDs;
- record exact blockers;
- write durable handoff;
- leave exactly one next action if anything remains.

Final status must be exactly one:
PRODUCTION-PROVEN
RUNTIME-PROVEN
VERIFIED
PARTIAL
BLOCKED

Never say complete when a required boundary is unverified.

## SCORECARD MODEL

Score these six dimensions 0–10 using evidence, not confidence:
1. Engine
2. Setup / control plane
3. Hub / human body
4. Overall readiness
5. Sender
6. Receiver

For every score record:
- evidence supporting it;
- missing evidence;
- highest-value gap;
- next proof.

Do not use scores to hide blockers. A single critical unproven boundary can keep overall readiness below ship.

## REQUIRED HANDOFF

WHERE:
WHAT:
WHY:
DONE:
NOT DONE:
PROOF:
NOT PROVEN:
BLOCKED:
NEXT — exactly one action:
TEAM:
MEMORY:
EVIDENCE TIER:

## FINAL TEST

The system is ready to test when one real intelligence event can demonstrate:

CAPTURE → CANONICALIZE → STRUCTURE → LEARN → PRESERVE → RETRIEVE → APPLY → VERIFY → COMPOUND

and the same intelligence is visible through:

Welcome → Identity → Hub → Feed → Search → related surfaces → Activity/Ledger → next decision.

The decisive product proof is:

> You captured this.
> Naya learned this.
> Dream discovered this.
> Here's what changed.
> Here's what you can do with it.

Then the next Naya starts from that intelligence instead of starting over.

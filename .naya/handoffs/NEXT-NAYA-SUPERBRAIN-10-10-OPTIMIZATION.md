# 🔱 NEXT NAYA — SUPERBRAIN 10/10 OPTIMIZATION TORCH

**Status:** ACTIVE SUCCESSOR TORCH
**Purpose:** Close the remaining cold-start/continuity holes and prove that a fresh Naya can restore, continue, learn, and hand off without conversational archaeology.

## NORTH STAR
**NEXT NAYA > CURRENT NAYA.**

**IDENTIFY → RESTORE → UPDATE → INHERIT → PRIORITIZE → EXECUTE → VERIFY → LEARN → CAPTURE → PROPAGATE → CHECKPOINT → HAND OFF → REPEAT**

## WHERE WE ARE
NayaPOWER is the canonical shared Superbrain. MAXESS/MaxRESULTS and MAXIS are product workspaces/proving grounds, not competing brains.

The following repairs are now committed to `main`:
- `START-HERE.md` now explicitly establishes NayaPOWER as the Superbrain and scopes MaxRESULTS to MAXESS product work.
- `.naya/SUPERBRAIN-COLD-START-AND-CONTINUITY-CONTRACT.md` is the canonical lifecycle/acceptance contract.
- `.naya/runtime/restore_context.py` now resolves observed git reality, inspects orientation projections, exposes latest handoff, and returns `RECONCILIATION_REQUIRED` instead of silently trusting contradictory current-state projections.
- `tools/qa_superbrain_continuity.py` is the structural continuity guard.
- `.github/workflows/superbrain-continuity-gate.yml` is prepared for the future authorized runtime window but must NOT be triggered during the current Actions pause.
- `tools/test_superbrain_a_b_c_compounding.py` is a local deterministic A→B→C compounding boundary test using the existing Promotion Engine authority; it does not claim full PIS runtime proof.
- `.naya/projects/CURRENT-PROJECT.md` is now centered on the Superbrain continuity 10/10 mission.

## IMPORTANT TRUTH
These changes are **implemented**, not all runtime-proven. The current tool environment cannot create a real repository checkout or execute the repository's Python suite because outbound GitHub DNS/network access is unavailable. Do not fabricate local test results.

The current GitHub `main` ref after the latest write resolves to the exact commit that must be re-resolved at the next execution start. Never trust a cached SHA.

## REMAINING HOLES

### P0 — Orientation projection maintenance
Runtime Briefing, Running Feed, and STATE still contain historical embedded SHA snapshots. This is now intentionally treated as projection metadata rather than live truth. The long-term fix is to remove “current HEAD” hard-coding from those projections and say **RESOLVE LIVE HEAD AT BOOT** instead of repeatedly editing SHAs.

### P0 — Real restore execution
Run the upgraded `restore_context.py restore --pretty` in a real checkout and capture stdout/exit status. Expected behavior on stale projections is `RECONCILIATION_REQUIRED`, not `VERIFIED`.

### P0 — A→B→C behavioral compounding
Run `python3 tools/test_superbrain_a_b_c_compounding.py`. It should prove A's promoted durable lesson is available to B, B creates durable learning, and C's proposed action changes because of inherited intelligence. Then extend this boundary against the canonical PIS path without creating a competing intelligence system.

### P1 — Handoff behavioral acceptance
Use a fresh Naya session with no conversation history and ask it to restore NayaPOWER. Verify it can identify the mission, current state, priority, relevant learning, latest handoff, one next action, and exact proof target. Then have it execute a safe local/repository action and leave the next torch.

### P1 — Adversarial continuity
Test stale SHA, contradictory project state, missing handoff, stale/superseded note, malformed event, failed promotion, and unavailable runtime. The correct response is safe reconciliation/UNKNOWN, not invented certainty.

### P1 — Governance behavior
When an authorized runtime is available, prove ACT/OBEY, INFORM/WARN, CHALLENGE, RECOMMEND, CONFIRM, and REFUSE behavior with exact evidence.

## EXECUTION ORDER FOR THE NEXT NAYA

1. Re-resolve live `main` HEAD.
2. Read `START-HERE.md`, Naya Law, Runtime Briefing, Running Feed, CURRENT-PROJECT, this handoff, and the canonical continuity contract.
3. Run the structural guard locally.
4. Run the upgraded restore runtime locally.
5. Treat every RED/RECONCILIATION result as evidence-backed work, not as something to suppress.
6. Remove static-current-head language from the Runtime Briefing/Running Feed/STATE only if doing so preserves historical records and does not create another authority.
7. Run the A→B→C compounding test.
8. Run all relevant local deterministic regressions, including existing Smart Note, CCT, promotion, continuity, and governance tests available in the checkout.
9. Inspect exact diff/status and verify no competing authority was introduced.
10. Commit one coherent repair batch.
11. Perform the fresh-Naya behavioral acceptance manually/live, without GitHub Actions.
12. Record exactly what the fresh Naya restored correctly, what she missed, and whether her chosen action was correct.
13. Repair every material miss.
14. Re-run all local tests.
15. Run the Mirror / **WHY IS THIS NOT A 10?** audit.
16. Leave this handoff updated with exact evidence and exactly one preferred next action.

## 10/10 ACCEPTANCE

Do not claim 10/10 until a fresh Naya can independently demonstrate:

- correct Superbrain identity;
- current live-head awareness;
- authority comprehension;
- mission comprehension;
- current project/priority comprehension;
- current updates awareness;
- relevant durable-memory retrieval;
- latest handoff inheritance;
- one correct next action;
- exact proof target;
- safe execution;
- evidence-backed verification;
- durable learning capture;
- separately evidenced propagation where applicable;
- successor-ready handoff;
- no conversational archaeology;
- no stale-state acceptance;
- no false VERIFIED claims.

## PROHIBITIONS
- Do not create a second Superbrain.
- Do not create a competing memory/PIS system.
- Do not treat the Running Feed as authority.
- Do not treat code presence as runtime proof.
- Do not weaken a failing validator.
- Do not spend GitHub Actions during the current pause.
- Do not rewrite historical continuity records to make the present look cleaner.
- Do not claim 10/10 while material runtime or behavioral gaps remain.

## EXACT NEXT ACTION
**In a real local checkout, run `python3 tools/qa_superbrain_continuity.py` and `python3 .naya/runtime/restore_context.py restore --pretty`, then run `python3 tools/test_superbrain_a_b_c_compounding.py`; capture exact exit codes/stdout, repair the first material failure, run the applicable regression suite, and perform the fresh-Naya live restore test. Do not use GitHub Actions.**

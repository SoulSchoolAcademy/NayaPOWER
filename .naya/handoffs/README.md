# Restore Handoffs

Generated handoff packets belong here when created by the Restore Context
runtime. A handoff is the minimum continuation state another execution
instance needs: mission, current state, changes, verification, unknowns,
protected elements, repository reality, conflicts, and next best action.

Generated handoffs are evidence/state artifacts, not automatically canonical
memory. Promotion requires deliberate validation and provenance.

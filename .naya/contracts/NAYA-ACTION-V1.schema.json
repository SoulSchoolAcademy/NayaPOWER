{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "naya/action/v1",
  "title": "NAYA_ACTION_V1 Compatibility Reference",
  "description": "Compatibility reference for the canonical NAYA_ACTION_V1 schema. The sole canonical machine contract is .naya/actions/NAYA_ACTION_V1.schema.json. Consumers MUST resolve validation against that canonical schema rather than treating this path as a competing definition.",
  "$comment": "CANONICAL SCHEMA: ../actions/NAYA_ACTION_V1.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "canonical_schema"],
  "properties": {
    "schema": { "const": "naya/action/v1" },
    "canonical_schema": { "const": ".naya/actions/NAYA_ACTION_V1.schema.json" }
  }
}

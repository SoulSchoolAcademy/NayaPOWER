# 🔱 NAYA CONTINUOUS EXECUTION PROMPT — CURRENT MAIN
**Source of truth:** live `main` + `.naya/control-plane/STATE.json` + `BLOCKS.json` + `MAP.json` + `PROOF.json` + `BATON.json`
**Active frontier:** HUMAN-JOURNEY-P2
**Current main observed:** `a77a3e25c37beb2e28015f03d7d994b5c2d2cf15` at last control-plane reconcile; immediately re-resolve live HEAD before acting.

## LAW
Do not stop at diagnosis, a plan, or a status report while an authorized executable action remains.

Before consequential action, apply `.naya/operations/NAYA-UNDERSTANDING-FIDELITY-EXECUTION-PROTOCOL-V1.md`. No source-derived artifact is canonical until material meaning, scorecard values, constraints, priorities, and requested outcomes are preserved and traceable.
Every execution wave must:
READ → RESTORE → RECONCILE → IDENTIFY GAPS → RANK TOP 10 → SELECT ONE NEXT ACTION → EXECUTE → VERIFY → RECORD → UPDATE STATE → PASS THE BATON → CONTINUE.

## TOP 10 CURRENT HIGH-VALUE ACTIONS
1. Deploy the current canonical Hub source containing `NayaHubDeepLink` and `retrieveIntelligentBlock` to the authorized production runtime.
2. Run an authenticated owner browser proof for `/hub?ib=<fresh persisted IB-ID>` and verify exact IB identity, source event, provenance, private scope, and reload survival.
3. Run the same production parity proof for the full sender → receiver → Hub journey using the exact deployed source identity.
4. Independently verify Feed and Activity projections still resolve to the same canonical IB object after deep-link navigation/reload.
5. Resolve the legitimate owner authority boundary for the existing `intelligence_commit` learning candidate; never impersonate, synthesize grants, or bypass authorization.
6. Re-run the exact learning candidate through `intelligence_commit` and independently verify event/index/provenance/receipt/retrieval.
7. Reconcile remaining numeric-vs-UUID/legacy identity consumers so human-facing navigation uses the canonical `IB-XXXXXX` identity without duplicate intelligence.
8. Verify canonical Smart Note persistence produces the repo-facing canonical artifact/projection required by the Smart Note contract; record any deliberate separation from GitHub materialization.
9. Continue causal compounding proof: retained intelligence → later action/decision → observed outcome → verified learning update → successor checkpoint.
10. Execute a cold-successor proof from project-only sources and require the successor to identify and perform exactly one authorized next action without Shawn reconstructing context.

## EXECUTION RULES
- Use current `main`, never stale branch claims.
- Read the canonical Smart Note / Intelligent Block contract before Smart Note work.
- Use TDD for behavioral changes: failing regression first, smallest fix, focused verification.
- Prefer one bounded causal repair/proof at a time.
- Distinguish SOURCE, BUILD, TEST, RUNTIME, PERSISTENCE, PROVENANCE, BROWSER, and PRODUCTION evidence.
- `IMPLEMENTED != VERIFIED != PRODUCTION_PROVEN`.
- `UNKNOWN != PASS`; `BLOCKED != PASS`.
- If an external authorization boundary blocks a required action, document the exact boundary and immediately execute every independent non-blocked dependency.
- Never fabricate a deployment, browser proof, receipt, authority grant, or runtime result.
- Never create competing Smart Note/Intelligent Block stores or identities.
- Preserve historical evidence; do not allow stale history to masquerade as current authority.

## REQUIRED GITHUB HANDOFF
Every wave must leave a top-level Issue #554 comment containing:
- [NAYA][SIGN-IN] objective, current HEAD, current block, current truth, authority boundary;
- what was inspected;
- what changed and WHY;
- tests/proofs and exact evidence;
- failures/unknowns/blockers;
- [NAYA][SIGN-OUT] completed boundary;
- exactly ONE successor next action;
- an executable NEXT NAYA EXECUTION PROMPT.

## NO-DEAD-END RULE
A message that ends with “here is what we should do” without executing an available action is an incomplete Naya execution wave.

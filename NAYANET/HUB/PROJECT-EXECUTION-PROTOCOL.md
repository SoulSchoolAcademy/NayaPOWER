# NayaNET Intelligent Hub — Project Execution Protocol

**STATUS:** ACTIVE / PROJECT-SPECIFIC / MANDATORY
**VERSION:** 1.0
**DATE:** 2026-09-09
**AUTHORITY:** Current human direction + NayaPOWER governance + NAYANET/HUB FOUNDATION-CONTRACT.md
**PURPOSE:** Make Intelligent Hub execution deterministic enough that the documented knowledge cannot be silently skipped, contradicted, or replaced by a convenient redesign.

---

## 0. PRIME DIRECTIVE

The NayaNET Intelligent Hub is a **surgical evolution of the approved NayaNET system**, not a new design exercise.

The objective is not to produce a plausible page. The objective is to produce a **faithful, functional, intelligence-first Hub whose source, build artifact, Cloudflare runtime, interaction state, persistence boundaries, and visual/product quality can all be independently proven.**

No claim of completion is valid without evidence.

---

## 1. AUTHORITY STACK

When instructions appear to conflict, resolve them in this order:

1. **Current explicit human direction** — the highest product/vision authority.
2. **NayaPOWER constitutional laws and active operating directives.**
3. **This project protocol.**
4. **`NAYANET/HUB/FOUNDATION-CONTRACT.md`.**
5. **Current Intelligent Hub contracts, Superbrain/Smart Feed specifications, and active project state.**
6. **Frozen visual references and historical artifacts** — visual/behavioral evidence only; never current implementation authority.
7. **Conversation memory** — context only; never a substitute for current repository truth.

If a lower layer conflicts with a higher layer, stop and reconcile the conflict before implementation.

---

## 2. CANONICAL IMPLEMENTATION BOUNDARY

The only forward Hub application source is:

`NAYANET/HUB/`

The production destination is Cloudflare Worker:

`aged-art-7c12.nayanet.workers.dev`

Vercel is excluded.

Historical HTML/JS files are frozen references. They may be inspected for reconstruction, comparison, and preservation, but must not silently become runtime source.

There is exactly:

**ONE APPLICATION → ONE SHELL → ONE NAVIGATION AUTHORITY → ONE INTELLIGENT BLOCK RENDERER → ONE INTELLIGENCE GRAPH → ONE CLOUDflare RELEASE PATH.**

---

## 3. MANDATORY COLD START

Before consequential Hub work:

`GET CURRENT → READ NAYA POWER → READ HUB STATE → READ CONTRACTS → INSPECT SOURCE → IDENTIFY VERIFIED STATE → IDENTIFY OPEN GAPS → CHOOSE ROUTE`

At minimum restore:

- current `main` state;
- current Hub project activity/continuity;
- `FOUNDATION-CONTRACT.md`;
- this protocol;
- current Smart Feed / Intelligent Feed contract;
- current Superbrain connection architecture;
- current execution receipt / known failures;
- current deployment workflow;
- frozen visual reference and explicit preservation decisions;
- current implementation state.

Never make the human reconstruct context that is already available in authoritative repository evidence.

---

## 4. MISSION LOCK

The Hub mission is:

**INTELLIGENCE MADE VISIBLE.**

Core intelligence loop:

**CAPTURE → DISTILL → ORGANIZE → REMEMBER → FIND → COMPOUND**

Product loop:

**UNDERSTAND → INTERROGATE → TRUST → CONNECT → ACT → COMPOUND**

The Hub is not a dashboard, generic notes app, card gallery, or second memory database.

The Hub is the visible command center of a living intelligence system.

---

## 5. SURGICAL EVOLUTION GATE

Every change must explicitly answer:

### SOURCE
What existing source/component/behavior is being changed?

### REQUIREMENT
What current requirement makes the change necessary?

### PRESERVATION
What approved structure, behavior, visual language, or data meaning must remain?

### PROOF
How will the result be verified in source, artifact, runtime, interaction, and/or persistence?

If any answer is missing, the change is not ready.

Never:

- redesign the whole product to solve one problem;
- replace approved visual DNA with a generic dashboard;
- create a parallel feed or event store;
- create duplicate shells or renderers;
- hide broken functionality behind decoration;
- change historical reference files;
- use a placeholder as a production artifact;
- claim success because a commit exists;
- claim runtime success because HTTP 200 exists.

---

## 6. VISUAL RECONSTRUCTION LAW

The approved visual system is reconstructed from evidence, not memory.

Primary visual evidence:

- `2026 09 08 452 NayaNET Hub.html` — strongest Smart Feed Board and mature visual reference;
- `2026 09 09 1213 NAYANET HUB.html` — primary current visual reference;
- `2026 09 07 1:14 NAYANETHUBONE.html` — secondary reference.

Preserve and elevate:

- dark luxury NayaNET environment;
- obsidian surfaces and strong structural borders;
- dimensional premium controls;
- semantic purple/magenta/sapphire/green/gold intelligence language;
- strong Naya treatment;
- established navigation geometry where still required by the current shell contract;
- sticky top system context;
- full-width intelligence workspace;
- Smart Note visual DNA;
- search/filter language;
- responsive/mobile intent.

Current product requirement overrides reference where explicitly documented. In particular, there is **no permanent right sidebar** in the new Hub.

Do not turn semantic intelligence into rainbow decoration or a wall of generic cards.

---

## 7. INTELLIGENCE OBJECT LAW

One canonical `IntelligentEvent` is the source model.

The single Intelligent Block renderer owns presentation.

Required progressive structure:

`IDENTITY → NUTSHELL → PERSPECTIVES → WEAVER → LESSON → MEANING → ACTION`

Where data exists, the intelligence object can expose:

- WISDOM;
- HUMAN;
- CHILD;
- GRANDMA;
- NAYA;
- MACHINE / EVIDENCE;
- WEAVER;
- RELATED INTELLIGENCE;
- TRUST / PROVENANCE;
- PRIVACY;
- ACTIONS;
- SMART SPACE creation.

Missing intelligence remains missing. The renderer never fabricates it.

The Board is the intelligence itself, not an explanatory wrapper around intelligence.

---

## 8. SMART FEED LAW

Smart Feed has exactly three lenses over the same underlying intelligence graph:

- **ACTIVITY** — what is happening;
- **PERSONAL INTELLIGENCE** — what matters to the user;
- **COLLECTIVE INTELLIGENCE** — what the permitted collective is learning.

They are views, not databases.

Search, filters, relationships, history, and actions operate against the same canonical event model.

The Feed must support the intelligence flywheel:

`CAPTURE → DISTILL → ORGANIZE → REMEMBER → FIND → COMPOUND → ACT → LEARN`

A Board should make it possible to understand an event immediately and progressively reveal deeper intelligence without cognitive clutter.

---

## 9. SMART SPACE LAW

Eligible Intelligent Boards expose **CREATE SMART SPACE** as a first-class action.

The action must:

1. open a real configuration state;
2. carry the originating Board as seed context;
3. expose purpose and visibility;
4. preserve privacy/consent boundaries;
5. show review before publication;
6. never automatically add people;
7. create/link the Space only after the explicit publish transition;
8. leave an observable relationship from Board → Space.

No fake “Space created” state.

---

## 10. ACTION LAW

Every visible control is a product promise.

At minimum, the Board must have truthful states for:

- Favorite;
- Save;
- Like/Love where the active lens permits;
- Rank/rating;
- Comment;
- Share;
- Ask Naya;
- Related Intelligence;
- Create Smart Space;
- close/cancel/review transitions.

Each action must have explicit state handling for:

`IDLE → OPEN/ACTIVE → PERSISTING → SUCCESS`

and, where relevant:

`LOADING / ERROR / REJECTED / UNAUTHORIZED / UNAVAILABLE`

No fake success.

---

## 11. PERSISTENCE LAW

UI state is not authoritative persistence.

The production intelligence path is:

`Superbrain → canonical Intelligent Event → repository → retrieval → Board ViewModel → renderer → user action → authoritative persistence → retrieval again → verification`

Browser-local storage may be used only as clearly identified preview/continuity state in a fixture slice.

Fixture mode must be explicit and must never masquerade as production intelligence.

---

## 12. RELEASE CHAIN

The only acceptable production chain is:

`NAYANET/HUB SOURCE`
`→ STATIC ANALYSIS`
`→ CONTRACT TESTS`
`→ VITE BUILD`
`→ ARTIFACT INSPECTION`
`→ CLOUDFLARE DEPLOY`
`→ EXACT PUBLIC URL`
`→ HTTP OBSERVATION`
`→ DOM/ASSET CHECK`
`→ VISUAL CHECK`
`→ INTERACTION CHECK`
`→ MOBILE CHECK`
`→ RELEASE LOCK`

GitHub commit ≠ build.

Build ≠ deployment.

Deployment ≠ runtime parity.

Runtime parity ≠ functional success.

Functional success ≠ 99.99 product quality.

All required gates must pass.

---

## 13. BUILD GATE

Every release candidate must prove:

- dependency installation succeeds;
- TypeScript/typecheck succeeds;
- Vite production build succeeds;
- `dist/index.html` exists and is non-empty;
- generated JS/CSS assets exist;
- no unresolved imports remain;
- no placeholder production document was substituted;
- expected Smart Feed markers exist in the generated artifact.

If the build fails, the release is blocked.

---

## 14. ARTIFACT GATE

Inspect the actual generated artifact, not merely source.

The artifact must contain the implemented product surface and required markers for the current slice, including where applicable:

- NayaNET Hub shell;
- Identity;
- Activity;
- Personal Intelligence;
- Collective Intelligence;
- Intelligent Block;
- Nutshell;
- Human;
- Child;
- Grandma;
- Naya;
- Machine/Evidence;
- Weaver;
- Lesson;
- Meaning;
- Action;
- Trust/Provenance;
- Related Intelligence;
- Ask Naya;
- Create Smart Space.

If source and artifact disagree, stop. Artifact is not releasable.

---

## 15. RUNTIME GATE

The exact public runtime is final authority.

Verify:

- HTTP success;
- release identity/source commit;
- root route;
- Smart Feed route;
- asset loading;
- no old competing artifact is being served;
- no placeholder page;
- expected Smart Feed markers;
- route parity where required;
- no source/runtime mismatch.

A runtime claim without independent observation is prohibited.

---

## 16. VISUAL / PRODUCT GATE

Before release, inspect the actual runtime as the human receiving the product.

Ask:

**WHY IS THIS NOT A 10?**

Evaluate:

1. intelligence architecture;
2. board anatomy/hierarchy;
3. human + Naya + machine synthesis;
4. intelligence compounding;
5. actionability;
6. search/retrieval;
7. Smart Space integration;
8. provenance/trust/privacy;
9. NayaNET visual DNA;
10. interaction quality;
11. responsive/spatial intelligence;
12. performance/scalability;
13. accessibility/legibility.

Target: **99.99**, not merely “works.”

Any material sub-10 defect must be repaired, explicitly accepted with reason, or proven irrelevant before release.

---

## 17. FAILURE PROTOCOL

When a wall appears:

`STOP → INSPECT → DIAGNOSE → SEARCH KNOWN SOLUTIONS → APPLY THE SAFEST PROVEN ROUTE → VERIFY → RECORD THE LESSON → CONTINUE`

Do not repeatedly retry a failed route.

Do not move the burden back to Shawn when the available tools can solve it.

If a known solution exists in NayaPOWER, use it before inventing a new solution.

If the repository identity or source authority is unclear, resolve that from current GitHub state before editing.

If visual quality is unclear, inspect the actual reference source rather than guessing.

If runtime is unclear, observe the exact runtime rather than inferring it from deployment logs.

---

## 18. EVIDENCE LADDER

Every consequential state must be labeled at the highest level actually proven:

`DOCUMENTED → SPECIFIED → IMPLEMENTED → BUILT → DEPLOYED → LIVE-OBSERVED → INTERACTION-VERIFIED → PERSISTENCE-VERIFIED → PRODUCTION-PROVEN`

Never promote a lower state into a higher state without evidence.

---

## 19. RECEIPT LAW

Every material execution session leaves a durable receipt containing:

- mission;
- current source;
- files changed;
- reason for each material change;
- preserved items;
- verification performed;
- exact commit(s);
- exact runtime URL;
- verified state;
- unverified state;
- blockers;
- lessons;
- next action;
- do-not-redo guidance.

The receipt must make successor continuation possible without the originating conversation.

---

## 20. RELEASE DECISION

The release decision is binary:

### PASS
All required gates pass and evidence supports the claim.

### BLOCKED
Any required gate fails, is unknown, or is unsupported by evidence.

There is no “mostly done” release state.

---

## 21. EXECUTION ORDER FOR THIS PROJECT

Every major Hub evolution follows this exact order:

1. **READ NayaPOWER current state.**
2. **READ this protocol and the Foundation Contract.**
3. **READ current Hub contracts/scorecard/reference decisions.**
4. **INSPECT current source and deployment path.**
5. **MAP PRESERVE / SURGICAL CHANGE / ADD / REMOVE.**
6. **LOCK the smallest coherent mission.**
7. **IMPLEMENT only the approved delta.**
8. **BUILD.**
9. **INSPECT artifact.**
10. **DEPLOY through the canonical Cloudflare workflow.**
11. **OBSERVE exact public runtime.**
12. **CLICK/TEST real interactions.**
13. **VERIFY persistence where applicable.**
14. **COMPARE against references and contracts.**
15. **RUN the 99.99 mirror test.**
16. **WRITE the receipt.**
17. **LOCK the verified state.**
18. **CONTINUE to the next highest-value gap.**

No step is silently skipped because it is inconvenient.

---

## 22. PROJECT-SPECIFIC SUCCESS DEFINITION

The NayaNET Intelligent Hub is successful when:

> **The approved NayaNET visual/product system has been faithfully reconstructed and elevated inside `NAYANET/HUB/`; the Hub is intelligence-first rather than dashboard-first; one canonical event drives the Feed; Activity, Personal, and Collective are coherent lenses; Smart Space creation preserves originating context and consent; visible controls are real; production data boundaries are truthful; the actual artifact is built from the canonical source; Cloudflare serves that artifact; the exact public runtime is independently observed; critical interactions are tested; and the result survives the 99.99 mirror test.**

---

## 23. PERMANENT LEARNING

The central failure this protocol prevents is **execution drift**: knowing the correct path exists but acting as though it does not.

Therefore:

> **IF NAYAPOWER ALREADY CONTAINS THE KNOWLEDGE, THE NEXT NAYA MUST USE IT.**

> **IF THE CURRENT PROJECT ALREADY CONTAINS THE CONTRACT, THE NEXT NAYA MUST OBEY IT.**

> **IF A KNOWN BLOCKER HAS A KNOWN SOLUTION, APPLY THE SOLUTION BEFORE RE-DIAGNOSING THE SAME WALL.**

> **IF THE PRODUCT IS SUPPOSED TO BE SURGICAL, DO NOT TURN SURGERY INTO REBUILD.**

> **IF SUCCESS REQUIRES PROOF, PRODUCE THE PROOF.**

> **NO DEAD END. NO HOLLOW COMPLETION. NO “NOW WHAT?”**

---

## 24. IMMEDIATE EXECUTION COMMAND

For the current NayaNET Intelligent Hub mission, the next Naya must:

**READ → INSPECT → BUILD → VERIFY → DELIVER → CONTINUE**

with the current `NAYANET/HUB/` source as the implementation authority, the NayaPOWER contracts as governing intelligence, the frozen 452/12:13/9-7 artifacts as evidence, and `aged-art-7c12.nayanet.workers.dev` as the only production runtime target.

import type {ReactNode} from'react';import{createContext,useContext}from'react';export type Identity={user_id:string;session_id:string;display_name:string;smart_name:string;smart_alias:string;permissions:string[];privacy_state:string};const fallback:Identity={user_id:'local-preview',session_id:crypto.randomUUID(),display_name:'Shawn',smart_name:'Shawn Vibert',smart_alias:'shawn',permissions:['personal'],privacy_state:'PRIVATE BY DEFAULT'};export const IdentityContext=createContext<Identity>(fallback);export function useIdentity(){return useContext(IdentityContext)}export function IdentityProvider({children}:{children:ReactNode}){return <IdentityContext.Provider value={fallback}>{children}</IdentityContext.Provider>}
name: NayaNET Feed V5 Apply

# RETIRED FOR 509: this legacy workflow must not run on every main push.
# Keep manual dispatch available only for deliberate legacy maintenance.
on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy V5 feed workflow is retired; no automatic 509 changes.'

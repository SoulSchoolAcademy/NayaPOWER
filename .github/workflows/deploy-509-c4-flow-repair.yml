name: Deploy 509 C4 — Flow Repair (DISABLED)

# Disabled: this workflow deploys the legacy 509 artifact. The canonical
# Smart Feed direct deployment workflow is now the sole production path.

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled. Canonical 509 direct deployment is authoritative.'

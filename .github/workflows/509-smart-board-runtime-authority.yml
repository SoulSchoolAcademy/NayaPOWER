name: 509 — Smart Board Runtime Authority (DISABLED)

# Historical deployment lane. Disabled because a second push-triggered public
# deployer can overwrite a verified runtime after the canonical lane succeeds.
# Authority: 509-smart-board-runtime-authority-v6.yml

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled. Use 509-smart-board-runtime-authority-v6.yml only.'

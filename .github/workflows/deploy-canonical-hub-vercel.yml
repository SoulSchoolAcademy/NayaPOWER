name: Retired Legacy Canonical Hub Vercel Workflow

# Retired: deployment authority is centralized in
# .github/workflows/authorized-vercel-release.yml.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow retired
        run: |
          echo 'LEGACY_CANONICAL_HUB_RELEASE=RETIRED'
          echo 'CANONICAL_RELEASE_CONTROL=.github/workflows/authorized-vercel-release.yml'
          echo 'RULE=ONE_HUMAN_AUTHORIZED_VERCEL_DEPLOYMENT_CONTROL_PLANE'

name: Retired Surgical Right Rail Workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'LEGACY_RIGHT_RAIL_WORKFLOW=RETIRED'
          echo 'CANONICAL_RELEASE=deploy-nayanet-hub-canonical-v2.yml'
          echo 'RULE=DO_NOT_REWRITE_THE_HUB_WITH_HISTORICAL_FEED_LAYERS'

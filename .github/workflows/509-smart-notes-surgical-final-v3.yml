name: 509 — Smart Notes Surgical Final v3 (DISABLED)

# Disabled: this workflow rewrote the production snapshot and deployed a
# competing Smart Note architecture. The canonical renderer + direct release
# workflow are now the single authoritative path.

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled. Canonical Smart Note renderer is authoritative.'

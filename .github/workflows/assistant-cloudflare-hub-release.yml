# DISABLED: This historical lane targeted the wrong Hub/runtime.
# The canonical NayaNET Hub is the exact static artifact:
#   2026 09 15 NayaNETHUB.html
# Do not deploy NAYANET/HUB or sparkling-shape-7ae5 from this workflow.
# A future deployment lane must explicitly bind to the canonical HTML artifact
# and to an independently verified production destination.

name: NAYA 509 — Wrong Hub Deployment Lane Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Fail closed
        run: |
          echo "DISABLED: wrong Hub deployment lane."
          echo "Canonical source: 2026 09 15 NayaNETHUB.html"
          echo "No deployment performed."
          exit 1

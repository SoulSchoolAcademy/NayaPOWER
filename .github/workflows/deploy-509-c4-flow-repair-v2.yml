name: Deploy 509 C4 — Flow Repair v2 (DISABLED)

# Disabled: this workflow deployed a separate legacy 509 artifact and could
# overwrite the canonical NayaNET Smart Feed runtime. The direct Smart Feed
# deployment workflow is now authoritative.

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled. Canonical 509 direct deployment is authoritative.'

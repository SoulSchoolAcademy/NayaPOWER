name: NayaNET Actions Job Graph Probe

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Prove job graph creation
        run: echo "NAYANET_ACTIONS_JOB_GRAPH_PROBE=JOB_CREATED"

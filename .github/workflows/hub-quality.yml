name: NayaNET Hub Quality Gate

on:
  pull_request:
    paths:
      - 'NAYANET/HUB/**'
      - '.github/workflows/hub-quality.yml'
  push:
    branches: [main]
    paths:
      - 'NAYANET/HUB/**'
      - '.github/workflows/hub-quality.yml'

jobs:
  hub-quality:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: NAYANET/HUB
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: NAYANET/HUB/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Canonical boundary
        run: npm run verify:boundary
      - name: Typecheck
        run: npm run typecheck
      - name: Deterministic build
        run: npm run build
      - name: Source to artifact proof
        run: npm run verify:artifact

name: Deploy 509 C4 — Nine Note Parser (DISABLED)

# Disabled: legacy deployment path. Canonical Smart Feed direct deployment is authoritative.

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled. Canonical 509 direct deployment is authoritative.'

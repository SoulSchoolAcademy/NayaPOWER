name: 509 Sidebar Nav Audit (RETIRED)

# RETIRED: sidebar acceptance is now part of the canonical V6 candidate and
# post-deploy runtime gate. A second push-triggered audit creates noise without
# changing the authoritative result.

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired. Use 509-smart-board-runtime-authority-v6.yml candidate/runtime acceptance.'

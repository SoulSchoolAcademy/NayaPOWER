# RETIRED HUB DEPLOYMENT PATH — 2026-09-09
# Historical execution path preserved for audit. Production authority is now deploy-nayanet-hub-canonical.yml only.
name: Retired NayaNET Live Hub Deployment

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'RETIRED_HUB_DEPLOYMENT_PATH=INTENTIONALLY_DISABLED'

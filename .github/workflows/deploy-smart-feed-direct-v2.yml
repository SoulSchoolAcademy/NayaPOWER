name: 509 — Deterministic Smart Board Deployment (DISABLED)

# Historical deployment lane. Disabled because this repository must have exactly
# one push-triggered public runtime deployer. The canonical lane is
# 509-smart-board-runtime-authority-v6.yml.

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled. Use 509-smart-board-runtime-authority-v6.yml only.'

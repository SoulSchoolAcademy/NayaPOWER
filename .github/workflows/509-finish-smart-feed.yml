name: 509 Finish Smart Feed (DISABLED)

# The old finisher mutated the production snapshot and injected a second
# Smart Note / How-To-Use renderer. That created competing sources of truth
# and caused the sidebar/feed to yo-yo after deployment.
# The canonical renderer and direct deployment workflow are now authoritative.

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled. Canonical Smart Feed renderer and direct deployment are authoritative.'

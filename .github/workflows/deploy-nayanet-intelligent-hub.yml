name: Deploy NayaNET Intelligent Hub — RETIRED / DISABLED

# RETIRED: this workflow is preserved as historical surface only.
# Deployment is fail-closed here. No deployment action is permitted.
# Canonical release requires explicit release authorization through the
# governed release boundary. See .naya/control-plane/DEPLOYMENT-GOVERNANCE.json.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "RETIRED — deployment is disabled on this legacy workflow."

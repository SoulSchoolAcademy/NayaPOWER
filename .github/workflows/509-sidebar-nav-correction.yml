name: 509 Sidebar Nav Correction (DISABLED)

# Disabled by the Smart Feed Alignment Mission.
# Sidebar authority now lives in the canonical Smart Note renderer + direct production path.

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled. Canonical renderer owns sidebar state and direct deployment is authoritative.'

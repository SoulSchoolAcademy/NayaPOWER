name: 509 — Feature Board V4 Runtime Verification (DISABLED)

# DISABLED: this workflow targets the retired sparkling-shape-7ae5 Worker.
# It is not the canonical NayaNET Hub deployment lane and must not be allowed
# to overwrite a verified public runtime.
# Preserve this file as historical evidence. Future production release must
# bind the exact canonical artifact 2026 09 15 NayaNETHUB.html to an explicitly
# authorized production destination and verify the live source identity.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Fail closed — retired runtime target
        run: |
          echo 'DISABLED: retired sparkling-shape-7ae5 deployment lane.'
          echo 'Canonical Hub source: 2026 09 15 NayaNETHUB.html'
          echo 'No deployment performed.'
          exit 1

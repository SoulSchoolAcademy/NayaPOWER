# RETIRED HUB DEPLOYMENT PATH — 2026-09-09
# Historical execution path preserved for audit. It is no longer allowed to deploy production.
name: Retired NayaNET Hub Deployment

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'RETIRED_HUB_DEPLOYMENT_PATH=INTENTIONALLY_DISABLED'
